# Data

Place the extracted CICIDS2017 CSV files in:

- Google Colab: `/content/cicids2017/`
- Local project run: `./cicids2017/`

Raw dataset files are intentionally not committed to this repository.

The notebook automatically discovers CSV files recursively.
