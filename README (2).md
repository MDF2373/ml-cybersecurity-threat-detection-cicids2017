# Results

This folder is reserved for final model outputs from the executed notebook.

Save at least:

- supervised model comparison metrics
- Random Forest classification report
- Random Forest confusion matrix
- unseen/novel Bot detection metrics
- hybrid detector comparison
- response-layer sample output, if desired

Do not enter or upload invented metrics. Only record values produced by the executed implementation.
